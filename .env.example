# 서버 포트 (기본 3000)
SERVER_PORT=3000

# DB 없이 실행 시: 아래 두 가지 중 하나만 적용하면 됨.
# 1) DATABASE_URL 을 비워두거나 설정하지 않음 → 앱이 더미 URL 로 Prisma 초기화만 하고 실제 DB 연결 안 함.
# 2) DB 연결 자체를 건너뛰려면 SKIP_DB_CONNECT=true
# SKIP_DB_CONNECT=true

# DB 사용 시: PostgreSQL 연결 문자열 (Prisma 사용)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
