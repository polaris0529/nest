import { GuestCookieInterceptor } from './guest-cookie.interceptor';

describe('GuestCookieInterceptor', () => {
  it('should be defined', () => {
    expect(new GuestCookieInterceptor()).toBeDefined();
  });
});
