import { PortainerInterceptor } from './portainer.interceptor';

describe('PortainerInterceptor', () => {
  it('should be defined', () => {
    expect(new PortainerInterceptor()).toBeDefined();
  });
});
